/*Cree un programa que imprima la tabla de multiplicar del 3, del 0 al 50. Use While*/

let tablas = 1

while (tablas<=50) {
    if (tablas *3  !=0 ){
        console.log(tablas);
    }
tablas++;
}